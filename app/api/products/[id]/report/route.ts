import {NextResponse} from "next/server";
import {ProductReportReason} from "@prisma/client";
import {prisma} from "@/lib/prisma";
import {readSession} from "@/lib/session";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const session=await readSession();if(!session)return NextResponse.json({error:"AUTH_REQUIRED"},{status:401});const {id}=await params;const body=await request.json();const reason=String(body.reason??"") as ProductReportReason,details=String(body.details??"").trim();if(!Object.values(ProductReportReason).includes(reason)||details.length<10||details.length>1500)return NextResponse.json({error:"INVALID_REPORT"},{status:400});const product=await prisma.product.findFirst({where:{id,status:"PUBLISHED"},select:{id:true,store:{select:{ownerId:true}}}});if(!product||product.store.ownerId===session.userId)return NextResponse.json({error:"NOT_REPORTABLE"},{status:400});await prisma.productReport.upsert({where:{productId_reporterId:{productId:id,reporterId:session.userId}},create:{productId:id,reporterId:session.userId,reason,details},update:{reason,details,status:"OPEN"}});return NextResponse.json({ok:true},{status:201})}
