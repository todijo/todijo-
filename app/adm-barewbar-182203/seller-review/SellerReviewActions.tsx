"use client";
import {useState} from "react";
export default function SellerReviewActions({storeId}:{storeId:string}){const[reason,setReason]=useState("");async function review(status:string){if(!reason.trim())return;const response=await fetch(`/api/admin/seller-onboarding/${storeId}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status,reason})});if(response.ok)location.reload()}return <div className="sellerReviewActions"><input value={reason} onChange={event=>setReason(event.target.value)} maxLength={500} aria-label="Review reason" placeholder="Review reason"/><button type="button" onClick={()=>review("VERIFIED")}>Verify</button><button type="button" onClick={()=>review("NEEDS_INFORMATION")}>Needs information</button><button type="button" onClick={()=>review("REJECTED")}>Reject</button></div>}
