import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";import test from "node:test";
test("Store Settings light surfaces use explicit readable foregrounds",()=>{const css=fs.readFileSync(path.join(process.cwd(),"app/globals.css"),"utf8");assert.match(css,/premiumDashboardContent:has\(\.storeSettingsForm\)/);assert.match(css,/--store-settings-text:#173b30/);assert.match(css,/\.storeSettingsForm \.sellerTypeSection h2/);assert.match(css,/\.storeSettingsForm \.sellerTypeChoices strong/);assert.match(css,/\.storeSettingsForm \.sellerTypeChoices small/);assert.match(css,/\.storeSettingsForm \.sellerTypeChoices label\.isSelected/);assert.match(css,/\.storeSettingsForm input::placeholder/);assert.match(css,/\.storeSettingsForm \.sellerControlActionBar/);assert.match(css,/\.sellerSettingsTabs a\{color:#526a60/)});
